:: EndOfDayHub
<<set $step = 0>><<ShowInfo>>
<<if $eodPage is undefined>><<set $eodPage = 1>><</if>><<set _maxEodPage = 2>>

<h2 style="margin:0;">End of Day – Day <<= $day>></h2>
<p style="font-size:0.8em;color:var(--muted);margin:0;">Page <<= $eodPage>> of <<= _maxEodPage>></p>

<<switch $eodPage>>

	<<case 1>>
		<<if $pendingPayment > 0>>
			<div style="margin:4px 0;padding:6px 8px;border-radius:6px;background:rgba(56,178,75,.08);border-left:3px solid rgba(56,178,75,.3);">
				<strong>💰 Payment: $<<= $pendingPayment>></strong><br>
				<<set $money += $pendingPayment>><<set $totalIncomeEarned += $pendingPayment>><<set $incomeHistory.push({amount:$pendingPayment,day:$day})>><<set $jobsCompleted += 1>>
				Balance: $<<= $money>> · Total: $<<= $totalIncomeEarned>> · Jobs: <<= $jobsCompleted>><<set $pendingPayment = 0>>
			</div>
		<<else>>
			<div style="margin:4px 0;padding:6px 8px;border-radius:6px;background:rgba(99,179,237,.08);border-left:3px solid rgba(99,179,237,.3);">
				<strong>💰 No new payment today.</strong>
			</div>
		<</if>>

		<div style="margin:4px 0;padding:6px 8px;border-radius:6px;background:rgba(208,213,224,.08);border-left:3px solid rgba(208,213,224,.3);">
			<<if $housingApplicationPending>>
				<strong>🏠 Application: <<= $pendingHousingType>></strong><br>
				<<if $housingApplicationDaysRemaining <= 0>>
					Decision ready. <<link "Check result" "HousingApplicationResult">><</link>>
				<<else>>
					~<<= $housingApplicationDaysRemaining>> business days left.
					<<set _isWeekend = ($currentDayOfWeek == 5 or $currentDayOfWeek == 6)>>
					<<if _isWeekend>><span style="font-size:0.8em;"> 📅 Weekend – no processing.</span><</if>>
				<</if>>
			<<elseif $hasHousing>>
				<strong>🏠 Housing:</strong> <<= $housingType>> · Rent: $<<= $housingCost>>/mo
			<<else>>
				<strong>🏠 No housing yet.</strong> · Days remaining: <<= $gameTimelineMax - $day>>
			<</if>>
		</div>

	<<case 2>>
		<div style="margin:4px 0;padding:6px 6px;border-radius:6px;background:rgba(236,201,75,.08);border-left:3px solid rgba(236,201,75,.3);">
			<strong>👨‍👩‍👧 Family:</strong> <<= $familyHealth>>%
			<<if $pregnancyProgress < $gameTimelineMax>>
				<<set _daysUntilBirth = $gameTimelineMax - $pregnancyProgress>> · Pregnancy: ~<<= _daysUntilBirth>> days until birth.
			<</if>>
			<<if $worries.length > 0>>
				<strong>⚠️ Worries: <<= $worries.length>></strong>
				<<for _i = 0; _i < $worries.length; _i++>>
					<<set _w = $worries[_i]>>· Sev <<= _w.severity>>/5 – <<= _w.description>> (<<= _w.daysActive>>d)
				<</for>>
			<<else>>
				<br>✓ No current worries.
			<</if>>
		</div>

		<div style="margin:4px 0;padding:6px 8px;border-radius:6px;background:rgba(99,179,237,.08);border-left:3px solid rgba(99,179,237,.3);">
			<strong>📊 Summary:</strong><br>
			Day <<= $day>> / <<= $gameTimelineMax>> · Left: <<= $gameTimelineMax - $day>><br>
			Money: $<<= $money>><br>
			<<if $job>>Job: <<= $jobName>> (<<= $workdays>>d left)<<else>>Job: none<</if>>
		</div>

		<div style="margin-top:4px;font-size:0.9em;">
			<<if $worries.length > 0>>
				<<link "Address family concerns (placeholder)">><<goto "Hub">><</link>><br>
			<</if>>
			<<link "Continue to next day" "Hub">><</link>><br>
			<<link "Game options" "GameOptions">><</link>>
		</div>

<</switch>>

<div style="display:flex;justify-content:space-between;font-size:0.85em;margin-top:4px;">
	<span>
		<<if $eodPage > 1>><<link "◀ Back">><<set $eodPage -= 1>><<goto "EndOfDayHub">><</link>><</if>>
	</span>
	<span>
		<<if $eodPage < _maxEodPage>><<link "Next ▶">><<set $eodPage += 1>><<goto "EndOfDayHub">><</link>><</if>>
	</span>
</div>
